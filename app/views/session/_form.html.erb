<%= form_with model: @user, url:  session_index_path, method: 'post' do |form| %>
  <%= form.text_field :phone_number, placeholder: 'Enter phone number' %>
  <%= form.submit 'Play Scrabble' %>
<% end %>